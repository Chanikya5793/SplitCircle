{
  "name": "splitcircle-category-classifier",
  "version": "1.0.0",
  "description": "BQML expense category classifier (MODEL-01) auto-categorize core + tests",
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@google-cloud/bigquery": "^7.9.0",
    "firebase-admin": "^13.6.0",
    "firebase-functions": "^7.0.5"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.9.2",
    "vitest": "^3.0.0"
  }
}
