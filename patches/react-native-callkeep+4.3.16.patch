diff --git a/node_modules/react-native-callkeep/.DS_Store b/node_modules/react-native-callkeep/.DS_Store
new file mode 100644
index 0000000..6308151
Binary files /dev/null and b/node_modules/react-native-callkeep/.DS_Store differ
diff --git a/node_modules/react-native-callkeep/android/.DS_Store b/node_modules/react-native-callkeep/android/.DS_Store
new file mode 100644
index 0000000..2e0d926
Binary files /dev/null and b/node_modules/react-native-callkeep/android/.DS_Store differ
diff --git a/node_modules/react-native-callkeep/android/src/main/java/io/wazo/callkeep/RNCallKeepModule.java b/node_modules/react-native-callkeep/android/src/main/java/io/wazo/callkeep/RNCallKeepModule.java
index 025480a..58167ee 100644
--- a/node_modules/react-native-callkeep/android/src/main/java/io/wazo/callkeep/RNCallKeepModule.java
+++ b/node_modules/react-native-callkeep/android/src/main/java/io/wazo/callkeep/RNCallKeepModule.java
@@ -434,7 +434,16 @@ public class RNCallKeepModule extends ReactContextBaseJavaModule implements Life
         this.hasListeners = false;
     }
 
-    @ReactMethod
+    // PATCHED (SplitCircle): @ReactMethod removed from this 3-arg overload.
+    // The New Architecture's TurboModule interop keys exported methods by NAME
+    // and rejects a module exporting two with the same one:
+    //   TurboModuleInteropUtils$ParsingException: Module exports two methods
+    //   to JavaScript with the same name: "displayIncomingCall"
+    // That throws while the module is REGISTERED, before any JS runs, so it is
+    // uncatchable and aborts the process (SIGABRT on mqt_v_js) on every launch
+    // — merely linking callkeep made the Android app unlaunchable.
+    // Java overloading is invisible to JS, so only one may be exported. Keep
+    // the 4-arg form: callkeep's own index.js calls exactly that on Android.
     public void displayIncomingCall(String uuid, String number, String callerName) {
         this.displayIncomingCall(uuid, number, callerName, false, null);
     }
@@ -483,7 +492,9 @@ public class RNCallKeepModule extends ReactContextBaseJavaModule implements Life
         conn.onAnswer();
     }
 
-    @ReactMethod
+    // PATCHED (SplitCircle): same duplicate-export fix as displayIncomingCall
+    // above. index.js calls the 4-arg form on Android
+    // (`RNCallKeepModule.startCall(uuid, handle, contactIdentifier, hasVideo)`).
     public void startCall(String uuid, String number, String callerName) {
         this.startCall(uuid, number, callerName, false, null);
     }
diff --git a/node_modules/react-native-callkeep/ios/.DS_Store b/node_modules/react-native-callkeep/ios/.DS_Store
new file mode 100644
index 0000000..0d77e33
Binary files /dev/null and b/node_modules/react-native-callkeep/ios/.DS_Store differ
diff --git a/node_modules/react-native-callkeep/ios/RNCallKeep/RNCallKeep.m b/node_modules/react-native-callkeep/ios/RNCallKeep/RNCallKeep.m
index 786045f..f0ccd27 100644
--- a/node_modules/react-native-callkeep/ios/RNCallKeep/RNCallKeep.m
+++ b/node_modules/react-native-callkeep/ios/RNCallKeep/RNCallKeep.m
@@ -1013,6 +1013,21 @@ continueUserActivity:(NSUserActivity *)userActivity
         INStartVideoCallIntent *startVideoCallIntent = (INStartVideoCallIntent *)interaction.intent;
         contact = [startVideoCallIntent.contacts firstObject];
     }
+#if __IPHONE_OS_VERSION_MAX_ALLOWED >= 130000
+    else {
+        // iOS 13+ Recents redials arrive as INStartCallIntent. When
+        // callCapability is INCallCapabilityUnknown (typical for third-party
+        // VoIP calls with generic handles), neither isAudioCall nor
+        // isVideoCall is set and the contact was never extracted — the tap
+        // silently did nothing. Fall through to INStartCallIntent.contacts.
+        if (@available(iOS 13, *)) {
+            if ([interaction.intent isKindOfClass:[INStartCallIntent class]]) {
+                INStartCallIntent *startCallIntent = (INStartCallIntent *)interaction.intent;
+                contact = [startCallIntent.contacts firstObject];
+            }
+        }
+    }
+#endif
 
     if (contact != nil) {
         handle = contact.personHandle.value;
